1. values added: 20
2. final result: 20
3. Using var to declare your variables in your programs led to naming conflicts and scoping issues. 
   
4. values added: 20
5. ReferenceError - This is because the variable result is declared inside the if block and cannot be accessed outside of it. Since we tried accessing it outside of it, we got an error
   
6. TypeError: Const cannot be changed it will always be 0 since we declared it.
7. Error: We are tried to reassign to a const variable so we got an error.

